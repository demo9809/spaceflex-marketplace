# Google Maps JavaScript API key — powers the real map on /drive-time.
# Without it, the drive-time page falls back to the illustrative map.
# Create a key in Google Cloud Console (enable "Maps JavaScript API"),
# restrict it to your domains, then copy this file to .env.local.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
